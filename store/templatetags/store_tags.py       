from django import template

register = template.Library()

def range_tags(value):
    """Range filter for generating list of numbers"""
    try:
        return range(1, int(value) + 1)
    except ValueError:
        return range(0)

register.filter('range_tags')